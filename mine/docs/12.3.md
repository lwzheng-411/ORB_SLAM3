# 硬件加速因子图优化系统

## 系统架构

```
ORB-SLAM3前端   GTSAM IMU预处理
     ↓               ↓
   Camera因子    IMU因子     Prior因子
        ↓          ↓            ↓
        └──────────┴────────────┘
                   ↓
              我的ASIC硬件
```

## EuRoC MH01 因子自动导出流程

Dataset -> Export(JSON) -> Import(Struct) -> Exporter -> Hardware Driver

1. **编译 ORB-SLAM3**  
   在 `ORB_SLAM3/` 下执行 `mkdir build && cd build && cmake .. && make -j`，新的 `Examples/Monocular-Inertial/mono_inertial_hw_export` 会与标准示例一起生成。

2. **准备输入**  
   - 词典：`ORB_SLAM3/Vocabulary/ORBvoc.txt`  
   - 配置：`ORB_SLAM3/Examples/Monocular-Inertial/EuRoC.yaml`（可按需调整）  
   - 数据集：解压 `MH01` 至 `ORB_SLAM3/dataset/MH01`，确保存在 `mav0/cam0/data/*.png` 与 `mav0/imu0/data.csv`  
   - 图像时间戳：`ORB_SLAM3/Examples/Monocular-Inertial/EuRoC_TimeStamps/MH01.txt`

3. **运行导出器**  
   ```bash
   ./Examples/Monocular-Inertial/mono_inertial_hw_export \
       Vocabulary/ORBvoc.txt \
       Examples/Monocular-Inertial/EuRoC.yaml \
       dataset/MH01 \
       Examples/Monocular-Inertial/EuRoC_TimeStamps/MH01.txt \
       /绝对路径/QR/output/mh01
   ```
   程序重放整条 MH01 序列，利用 Monocular-Inertial ORB-SLAM3 前端生成关键帧与地图点，并在离线阶段遍历 Atlas 提取 Camera / IMU / Prior 因子。

4. **输出文件（JSON）**  
   - `poses.json`：`pose_id`、时间戳、`Rcw`、`tcw`  
   - `camera_observations.json`：每条观测的 `pose_id`、`landmark_id`、像素、`Rcw`、`tcw`、`landmark_w`、内参、`sigma_pixel`  
   - `imu_edges.json`：相邻 pose 的原始 IMU 序列（`dt`、`acc`、`gyro`），用于后续 GTSAM 预积分  
   - `priors.json`：首个 pose 的 6DoF 先验（可拓展为更多帧）  
   - `summary.json`：整体统计（pose/因子数量、数据集路径等）

5. **接入硬件链路**  
   - 解析 JSON 后构造 `CameraObservation` / `ImuConstraint` / `PriorConstraint`，按 `pose_id` 调用 `VariableEliminationDriver` 即可驱动 `Axb → RowBuffer → SystolicArray`。  
   - `imu_edges.json` 中的序列直接喂给 `VariableEliminationDriver::makeImuConstraint()`，由 `ImuPreprocessorGTSAM` 统一白化。  
   - 若只需滑窗内因子，可在解析阶段筛选 `pose_id`，或仅保留前 N 帧进行快速验证。


梳理完整工作流程：

完整工作流程概览：

## 完整工作流程（Workflow）

### 阶段 1：数据准备与前端处理（已完成）

```
EuRoC MH01 数据集
├── 图像序列 (mav0/cam0/data/*.png)
├── IMU 数据 (mav0/imu0/data.csv)
└── 时间戳文件 (EuRoC_TimeStamps/MH01.txt)
         ↓
ORB-SLAM3 Monocular-Inertial 前端
├── 特征提取与匹配
├── 关键帧选择
├── 地图点构建
└── 位姿估计
         ↓
mono_inertial_hw_export 程序
├── 遍历 Atlas 提取关键帧
├── 收集地图点观测
├── 提取 IMU 原始数据
└── 序列化为 JSON
```

当前状态：已完成，生成 5 个 JSON 文件

---

### 阶段 2：JSON 解析与因子构造（待实现）

```
JSON 文件 (QR/output/mh01/)
├── poses.json (254 poses)
├── camera_observations.json (59,979 observations)
├── imu_edges.json (253 IMU segments)
├── priors.json (1 prior)
└── summary.json
         ↓
JSON 解析器 (待实现)
├── 读取 JSON 文件
├── 解析为 C++ 结构体
└── 构造输入结构
         ↓
ORBSLAM3Exporter 输入结构
├── ObservationInput (from camera_observations.json)
├── ImuEdgeInput (from imu_edges.json)
└── PriorInput (from priors.json)
```

需要实现：JSON 解析器，将 JSON 转换为 `ORBSLAM3Exporter` 的输入格式

---

### 阶段 3：因子图构建与 GTSAM 预积分（部分完成）

```
ORBSLAM3Exporter
├── addObservation(ObservationInput)
│   └── 转换为 CameraObservation
│       └── 存储到 landmark_observations_
│
├── addImuEdge(ImuEdgeInput)
│   └── VariableEliminationDriver::makeImuConstraint()
│       └── ImuPreprocessorGTSAM::preintegrate()
│           └── GTSAM IMU 预积分
│               └── 生成 ImuConstraint
│
└── addPrior(PriorInput)
    └── 转换为 PriorConstraint
         ↓
内部数据结构
├── landmark_observations_ (按 landmark_id 组织)
├── imu_constraints_ (IMU 约束列表)
└── priors_ (先验约束)
```

当前状态：`ORBSLAM3Exporter` 已实现，但需要 JSON 解析器来填充数据

---

### 阶段 4：变量消元（Variable Elimination）（已实现接口）

```
ORBSLAM3Exporter
├── eliminateLandmark(landmark_id)
│   └── VariableEliminationDriver::eliminateLandmark()
│       └── 收集该 landmark 的所有观测
│           └── 构建 Schur complement
│               └── 生成新的约束
│
└── eliminatePose(pose_id)
    └── VariableEliminationDriver::eliminatePose()
        └── 收集该 pose 的所有约束
            ├── IMU 约束
            └── Prior 约束
                └── 消元并生成新约束
```

当前状态：接口已实现，等待数据输入

---

### 阶段 5：硬件加速计算（已实现）

```
VariableEliminationDriver
         ↓
Axb 模块 (矩阵向量乘法)
├── 计算 A * x = b
└── 处理消元后的线性系统
         ↓
RowBuffer (行缓冲)
├── 管理矩阵行数据
└── 优化数据流
         ↓
SystolicArray (脉动阵列)
├── 并行矩阵运算
└── 硬件加速计算
```

当前状态：硬件模块已实现

---

## 当前工作流程状态

### 已完成
1. ORB-SLAM3 前端处理
2. JSON 数据导出（5 个文件）
3. `ORBSLAM3Exporter` 类实现
4. `VariableEliminationDriver` 接口
5. 硬件模块（Axb、RowBuffer、SystolicArray）

### 待实现
1. JSON 解析器
   - 读取 JSON 文件
   - 转换为 `ORBSLAM3Exporter::ObservationInput` 等结构
   - 调用 `exporter.addObservation()` 等接口

2. 工作流整合脚本
   - 自动解析 JSON
   - 调用 `ORBSLAM3Exporter`
   - 执行消元流程
   - 驱动硬件模块

---

## 下一步建议

创建一个 JSON 解析器程序，例如：

```cpp
// json_to_exporter.cpp
// 1. 读取 JSON 文件
// 2. 解析为 ObservationInput / ImuEdgeInput / PriorInput
// 3. 调用 ORBSLAM3Exporter 接口
// 4. 执行消元流程
```

需要我帮你实现这个 JSON 解析器吗？